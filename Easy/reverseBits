class Solution:
    # @param n, an integer
    # @return an integer
    def reverseBits(self, n):
        res = 0
        for i in range(32):
            # Gets bit at index i
            bit = (n>>i) & 1

            # Sets the bit at index 31-i
            res = res | (bit << (31-i))
        return res